security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build|uploads)/
            security: false

        login:
            pattern: ^/api/auth/login
            stateless: true
            security: false

        register:
            pattern: ^/api/auth/register
            stateless: true
            security: false

        api:
            pattern: ^/api/
            stateless: true
            jwt: ~
            provider: app_user_provider

        public_api:
            pattern: ^/api/public/
            stateless: true
            security: false

    access_control:
        - { path: ^/api/auth/login, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/api/auth/register, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/api/auth/me, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/api/themes, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/api/section-pages, roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/api/subscription, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/api/users, roles: ROLE_ADMIN }
        - { path: ^/api/categories, roles: ROLE_ADMIN }
        - { path: ^/api/albums, roles: ROLE_ADMIN }
        - { path: ^/api/media, roles: ROLE_ADMIN }
        - { path: ^/api/contacts, roles: ROLE_ADMIN }

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
